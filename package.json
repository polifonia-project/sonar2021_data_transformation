{
  "name": "sonar-demo-etl",
  "version": "0.0.0",
  "description": "ETL to transform Polifonia KG data and feed sonar demo app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "build": "npm test && rm -rf ./build/* && tsc",
    "postbuild": "copyfiles -u 1 src/**/*.{sparql,ttl} build",
    "tsc": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@comunica/actor-init-sparql": "^1.22.1",
    "@comunica/actor-init-sparql-file": "^1.22.2",
    "reflect-metadata": "^0.1.13",
    "typedi": "^0.10.0"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "copyfiles": "^2.4.1",
    "jest": "^27.2.4",
    "ts-jest": "^27.0.5",
    "tsc-watch": "^4.5.0",
    "typescript": "^4.4.3"
  }
}
